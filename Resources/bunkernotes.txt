# Build Notes

## Puny Inform Build
brew install inform6 # if needed
brew install --cask inform # if needed
brew install virtual-ii # if wanted
# punyInform is available here - https://github.com/johanberntsson/PunyInform/releases

# TinyZ Build
xcode-select --install # if not already installed
brew install bison acme
git clone https://github.com/etherton/tinyz
cd tinyz
make
